server:
  port: 9081
  servlet:
    context-path: /target-service

spring:
  application:
    name: target-service
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:alisa/smallokr/mapper/xml/*.xml
