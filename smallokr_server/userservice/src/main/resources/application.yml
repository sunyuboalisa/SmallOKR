server:
  port: 8084
spring:
  application:
    name: user-service
  config:
    import: optional:file:./smallokr_server/userservice/.env[.properties]
  profiles:
    active: dev
mybatis:
  mapper-locations: classpath:alisa/smallokr/mapper/xml/*.xml
